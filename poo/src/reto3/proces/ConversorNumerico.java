package reto3.proces;

/**
 * esta clase tiene metodo para convertir numeros de decimal a binario
 */
public class ConversorNumerico {
    /**
     * este metodo recibe un string para recibir  un numero en decimal
     * para regresar el mismo numero en vinario
     * @param decimal numero o convertir vase a deciamal
     * @return num en base binario
     */
    public static String decimalabinario(String decimal){
return ;
    }
}
